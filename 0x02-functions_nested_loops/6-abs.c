#include "main.h"

/**
 * _abs - Return the absolute value of a number
 * @n: number to convert
 * Return: absolute of n
 * author: Akpan Daniel
 */
int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
